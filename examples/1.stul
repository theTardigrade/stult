PRINT = STD["IO"]["PRINT"]
PI = STD["MATH"]["PI"]
SQ = STD["MATH"]["SQUARE"]
T = STD["BOOL"]["TRUE"]

PRINT(SQ(PI)) || This prints the result of π multiplied by itself.

is_pi_bigger_than_three = (PI > 3)

PRINT(is_pi_bigger_than_three) || Yes, it is!